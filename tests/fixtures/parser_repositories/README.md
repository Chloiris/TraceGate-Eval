# Parser-only repositories

These minimal sources are copied into temporary Git repositories exclusively
by parser/index/graph tests. They are not production demo data and must never
be presented as a real analyzed repository or real model result.
