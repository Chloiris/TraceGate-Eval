import { useDiagnostics, useUpdateStatus } from "../api/queries";
import { ErrorState, LoadingState } from "../components/RequestState";
import { StatusCard } from "../components/StatusCard";
import { errorMessage } from "../lib/errors";
import { useI18n } from "../i18n";

export function DiagnosticsPage() {
  const { locale, text } = useI18n();
  const diagnostics = useDiagnostics();
  const updateStatus = useUpdateStatus();
  if (diagnostics.isPending) return <LoadingState label={text("正在读取脱敏诊断信息…", "Reading redacted diagnostics…")} />;
  if (diagnostics.isError) return <ErrorState title={text("诊断信息不可用", "Diagnostics unavailable")} message={errorMessage(diagnostics.error)} onRetry={() => void diagnostics.refetch()} />;
  const data = diagnostics.data;
  return <div className="page-stack"><header className="page-header"><span className="eyebrow">LOCAL DIAGNOSTICS</span><h2>{text("诊断", "Diagnostics")}</h2><p>{text("诊断响应不包含 API Token、模型密钥或 GitHub Token；Telemetry 固定为默认关闭。", "Diagnostic responses contain no API, model, or GitHub tokens; telemetry remains off by default.")}</p></header><div className="status-grid status-grid-two"><StatusCard eyebrow="GITHUB" title="GitHub" status={data.github} /><StatusCard eyebrow="MODEL" title={text("模型", "Model")} status={data.model} /></div><section className="panel"><div className="section-heading"><div><span className="eyebrow">RUNTIME</span><h2>{text("版本与进程", "Versions and process")}</h2></div><button className="button button-secondary button-small" type="button" onClick={() => void diagnostics.refetch()}>{text("刷新", "Refresh")}</button></div><dl className="metadata-grid"><div><dt>{text("软件版本", "Software version")}</dt><dd>{data.software_version}</dd></div><div><dt>Git Commit</dt><dd className="mono">{data.git_commit ?? text("不可用", "Unavailable")}</dd></div><div><dt>OS / {text("架构", "architecture")}</dt><dd>{data.operating_system} / {data.architecture}</dd></div><div><dt>Python</dt><dd>{data.python_version}</dd></div><div><dt>Frontend / Desktop</dt><dd>{data.frontend_version ?? text("不可用", "Unavailable")} / {data.desktop_version ?? text("不可用", "Unavailable")}</dd></div><div><dt>Sidecar PID / Port</dt><dd>{data.sidecar_pid} / {data.api_port}</dd></div></dl>{updateStatus.data ? <p className={updateStatus.data.configured ? "inline-success" : "blocker-note"}>{text("更新通道", "Update channel")}: {updateStatus.data.channel} · {updateStatus.data.message}</p> : null}</section><section className="panel"><span className="eyebrow">STORAGE</span><h2>{text("本地数据", "Local data")}</h2><dl className="metadata-grid"><div><dt>{text("数据库类型", "Database type")}</dt><dd>{data.database_type}</dd></div><div><dt>{text("数据库路径", "Database path")}</dt><dd className="mono">{data.database_path ?? text("服务端模式不公开连接字符串", "Server mode does not expose the connection string")}</dd></div><div><dt>{text("日志路径", "Log path")}</dt><dd className="mono">{data.log_path}</dd></div><div><dt>Telemetry</dt><dd>{data.telemetry_enabled ? text("已开启", "Enabled") : text("关闭（默认）", "Off (default)")}</dd></div></dl><details><summary>{text("已授权工作区", "Authorized workspaces")}</summary><ul className="plain-list">{data.workspace_paths.map((path) => <li className="mono" key={path}>{path}</li>)}</ul></details></section><section className="panel"><span className="eyebrow">QUEUES</span><h2>{text("后台队列", "Background queues")}</h2><div className="metric-cards"><div><span>Monitor</span><strong>{data.monitor.running ? (data.monitor.polling ? "polling" : "idle") : "stopped"}</strong></div><div><span>Repository queue</span><strong>{data.monitor.queued_repositories}</strong></div><div><span>Agent queue</span><strong>{data.agent_queue}</strong></div><div><span>Index queue</span><strong>{data.index_queue}</strong></div></div>{data.monitor.last_error ? <p className="inline-error">{data.monitor.last_error}</p> : null}<p className="field-note">{text("最近轮询", "Last poll")}: {data.monitor.last_finished_at ? new Date(data.monitor.last_finished_at).toLocaleString(locale) : text("尚未运行", "Not run yet")}</p></section></div>;
}
