import { useMemo, useState } from "react";

import { useEvaluations } from "../api/queries";
import { ErrorState, LoadingState } from "../components/RequestState";
import { errorMessage } from "../lib/errors";
import { useI18n } from "../i18n";

export function EvalCenterPage() {
  const { text } = useI18n();
  const evaluation = useEvaluations();
  const [context, setContext] = useState("all");
  const [status, setStatus] = useState("all");
  const visible = useMemo(() => evaluation.data?.cases.filter((item) => (context === "all" || item.context_group === context) && (status === "all" || item.evidence_status === status)) ?? [], [context, evaluation.data?.cases, status]);
  if (evaluation.isPending) return <LoadingState label={text("正在验证 TraceGate Eval 与 ClaimBench 产物…", "Verifying TraceGate Eval and ClaimBench artifacts…")} />;
  if (evaluation.isError) return <ErrorState title={text("Eval 产物不可用", "Eval artifacts unavailable")} message={errorMessage(evaluation.error)} onRetry={() => void evaluation.refetch()} />;
  const data = evaluation.data;
  return <div className="page-stack"><header className="page-header"><span className="eyebrow">REAL BENCHMARK EVIDENCE</span><h2>Eval Center</h2><p>{data.benchmark_note}</p></header><section className="hero-panel compact-hero"><div><span className="eyebrow">{data.is_real_dataset ? "REAL DATASET" : "INVALID"}</span><h2>{data.benchmark_name}</h2><p className="mono">dataset sha256 {data.dataset_sha256}</p></div><div className="system-verdict"><span>Real cases</span><strong>{data.case_count}</strong><small>{data.claimbench_run_count} ClaimBench runs</small></div></section><section><div className="section-heading"><div><span className="eyebrow">EVIDENCE STATUS</span><h2>{text("真实分布", "Real distribution")}</h2></div></div><div className="metric-cards">{Object.entries(data.status_distribution).map(([key, value]) => <div key={key}><span>{key}</span><strong>{value}</strong><div className="metric-bar"><i style={{ width: `${value / data.case_count * 100}%` }} /></div></div>)}</div></section><section><div className="section-heading"><div><span className="eyebrow">METRICS</span><h2>{text("原始指标", "Raw metrics")}</h2></div></div><div className="metric-cards">{Object.entries(data.metrics).map(([key, value]) => <div key={key}><span>{key.replaceAll("_", " ")}</span><strong>{(value * 100).toFixed(2)}%</strong></div>)}</div></section><section className="panel"><div className="section-heading"><div><span className="eyebrow">CASE DRILL-DOWN</span><h2>{text("ClaimBench 运行记录", "ClaimBench run records")}</h2></div><button className="button button-secondary button-small" type="button" onClick={() => { const anchor = document.createElement("a"); anchor.href = URL.createObjectURL(new Blob([JSON.stringify(data, null, 2)], { type: "application/json" })); anchor.download = "tracegate-evaluation.json"; anchor.click(); URL.revokeObjectURL(anchor.href); }}>{text("导出报告", "Export report")}</button></div><div className="filter-bar"><label className="field"><span>Context</span><select value={context} onChange={(event) => setContext(event.target.value)}><option value="all">{text("全部", "All")}</option>{Object.keys(data.context_groups).map((item) => <option key={item} value={item}>{item}</option>)}</select></label><label className="field"><span>Evidence status</span><select value={status} onChange={(event) => setStatus(event.target.value)}><option value="all">{text("全部", "All")}</option>{Object.keys(data.status_distribution).map((item) => <option key={item} value={item}>{item}</option>)}</select></label><span className="count-label">{visible.length} rows</span></div><div className="data-table-wrap eval-table"><table className="data-table"><thead><tr><th>Task</th><th>Model</th><th>Context</th><th>Evidence</th><th>Expected / Actual</th><th>Safe</th><th>Tokens</th></tr></thead><tbody>{visible.map((item, index) => <tr key={`${item.task_id}-${item.context_group}-${index}`}><td>{item.task_id}</td><td>{item.model}</td><td>{item.context_group}</td><td>{item.evidence_status}</td><td>{item.expected_decision} / {item.decision}</td><td>{item.safe_success ? "yes" : "no"}</td><td>{item.context_tokens}</td></tr>)}</tbody></table></div></section><section className="panel"><span className="eyebrow">LIMITATIONS</span><ul className="plain-list">{data.limitations.map((item) => <li key={item}>{item}</li>)}</ul><details><summary>{text("已验证 Artifact", "Verified artifacts")}</summary><ul className="plain-list">{data.artifacts.map((item) => <li key={item.path}><span className="mono">{item.path}</span><br /><small className="mono">{item.sha256}</small></li>)}</ul></details></section></div>;
}
